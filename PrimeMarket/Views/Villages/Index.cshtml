@using PagedList;
@using PagedList.Mvc;
@model IPagedList<PrimeMarket.Models.Village>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/LayoutAdmin.cshtml";
}
<br /><br /><br />
<h2>القرى</h2>
<link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
<p>
    @if (Request["DistrictId"] != null)
{
    @*@Html.ActionLink("اضافة قرية جديدة", "Create", new { DistrictId = Request["DistrictId"], GovernorateId = Request["GovernorateId"] }, new { @class = "site-btn" })*@
    @Html.ActionLink("اضافة قرية جديدة", "Create", null, new { @class = "site-btn" })
}
else
{
    @Html.ActionLink("اضافة قرية جديدة", "Create", null, new { @class = "site-btn" })
    @*@Html.ActionLink("اضافة قرية جديدة", "Create", new { DistrictId =1, GovernorateId = Request["GovernorateId"] }, new { @class = "site-btn" })*@
}
</p>
<table id="villagetbl" class="disply table-responsive">
    <thead>
        <tr>
            @*<th>المحافظة</th>*@
            <th>
                المركز

            </th>
            <th>
                القرية/النجع
            </th>

            <th></th>
        </tr>
        </thead>
    <tbody>

        @foreach (var item in Model)
        {
            <tr>
                @*<td>
                    @Html.DisplayFor(modelItem => item.Governorate.Governorate1)*@
                @*@Html.Display("Governorate",null, new { districtId = item.DistrictId , @class = "form-control" })
                    </td>*@
                <td>
                    @Html.DisplayFor(modelItem => item.District.District1)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Village1)
                </td>

                <td>
                    <i class="large material-icons btn-default">
                        @Html.ActionLink("edit", "Edit", new { id = item.VillageId }, new { @title = "تعديل ", @style = "color:gray" })
                    </i>
                    |
                    <i class="large material-icons ">
                        @Html.ActionLink("storage", "Details", new { id = item.VillageId }, new { @title = "تفاصيل ", @style = "color:gray" })
                    </i> |
                    <i class="large material-icons"> @Html.ActionLink("delete", "Delete", new { id = item.VillageId }, new { @title = "حذف ", @style = "color:#a50303" }) </i>
                </td>

             
            </tr>
        }
    </tbody>
</table>
@*@Html.PagedListPager(Model, page => Url.Action("Index", new { page, @class = "site-btn" }))*@
<br />
<div>
    @Html.ActionLink("عودة للمراكز", "Index", "Districts", null, new { @class = "site-btn" })


</div>
@section Scripts{
<script>
        $(document).ready(function () {
            $('#villagetbl').DataTable({
                "language": {
                    "url": "/content/arabic.json"
                }
            });
        });
</script>
    }